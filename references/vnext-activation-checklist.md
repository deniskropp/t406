# MetaForge Orchestrator vNext Activation Checklist

**Version**: 1.0  
**Purpose**: Lightweight, repeatable process to activate documentation-aware mode, DocWeaver, Fizz La Metta vNext, and the two core vNext protocols (`doc-integration v1.0` + `prompt-evolution v1.0`) on any skill, substrate, forge, or workflow artifact.

---

## Prerequisites
- `metaforge-orchestrator-vnext` skill activated (loads t402 generated-extensions.md context)
- Target artifact(s) identified (e.g. SKILL.md, ocs-substrate.md, blueprint.md, README.md)
- GitHub write access to the target repository
- Current Meta-DNA / Denis Kropp DNA alignment confirmed

## Activation Steps (in order)

1. **Load vNext Context**  
   Explicitly internalize:
   - `⫻protocol/metaforge/doc-integration v1.0`
   - `⫻protocol/prompt-evolution v1.0`
   - DocWeaver agent prompt
   - Fizz La Metta vNext prompt
   - Lyra documentation-augmented extension

2. **Add Documentation Reference Trigger**  
   Insert a clear `⫻cmd/doc-reference` step early in the Core Protocol / Execution flow (immediately after objective ingestion is recommended).

3. **Mandate ⫻data/doc-trace**  
   Require `⫻data/doc-trace` in:
   - Authoritative state emission (`⫻data/state`)
   - Key post-cycle integration outputs
   - Any major coordination or synthesis result

4. **Extend Guardrails**  
   Add `on_documentation_drift` rule:
   > DocWeaver + KickGuard review; trigger prompt-evolution protocol if recurring friction or opportunity detected.

5. **Update Team & Integration Section**  
   Explicitly list:
   - **DocWeaver** (vNext) — documentation bridge + trace provider
   - **Fizz La Metta vNext** (vNext) — living coordinator + real-time substrate monitor
   - Reference to the two new protocols

6. **Add Dedicated vNext Integration Subsection**  
   Insert a new top-level section:
   ```markdown
   ## MetaForge Orchestrator vNext Integration (YYYY-MM-DD)
   **Status**: Activated via checklist v1.0  
   **⫻data/doc-trace**: t402 generated-extensions.md + this checklist  
   **Capabilities live**: `⫻cmd/doc-reference`, `⫻data/doc-trace`, DocWeaver synthesis, Fizz La Metta vNext coordination, prompt-evolution hooks  
   **Alignment**: Target now shares documentation layer with t402 and active substrates (e.g. t406)
   ```

7. **Refresh Usage Notes & Resources**  
   - Add note about evolving via `metaforge-orchestrator-vnext`
   - Link to t402 generated-extensions.md and this checklist
   - Mention documentation-aware mode is now native

8. **Version Bump & Coherence Gate**  
   - Increment version (e.g. v0.6 → v0.7 or add vNext suffix)
   - Run quick KickGuard coherence review against Meta-DNA and Denis Kropp DNA

9. **Push to GitHub**  
   Use clear commit message that includes `⫻data/doc-trace` reference and the activation event.

10. **Post-Activation Validation** (optional but recommended)
    - Run a small documentation-aware test cycle (if the artifact supports forecasting)
    - Update linked substrates (e.g. ocs-substrate.md)
    - Broadcast activation event via Orchestrator

## Template Usage
Copy this checklist into the target repository’s `references/` folder (or equivalent) and mark completed steps. Reuse across the tXXX ecosystem and new forges.

## Notes
- Keep the checklist lightweight and non-breaking.
- Always preserve consent-first, coherence-gated, and Denis Kropp DNA principles.
- This checklist itself was generated and deployed via DocWeaver + metaforge-orchestrator-vnext on t406.

---
**Maintained by the Orchestrator** | Part of MetaForge vNext evolution layer